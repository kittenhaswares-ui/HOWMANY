# Privacy

HOWMANY does not send anything over the network and has no backend or community database.

The plugin reads only the local game state needed to render its PvP overlay: whether the player is in PvP, visible hostile player objects, their current hard-target IDs, and their job IDs. It does not save those observations. Dalamud stores only the user's local display settings.
